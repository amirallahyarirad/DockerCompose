services:
  excalidraw:
    image: excalidraw/excalidraw:latest
    container_name: excalidraw
    ports:
      - "3000:80"
    volumes:
      - excalidraw-data:/usr/share/nginx/html   # future data storage
    restart: unless-stopped

volumes:
  excalidraw-data: